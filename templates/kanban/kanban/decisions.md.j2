# Decision Log - {{ project_name }}

```yaml
section: kanban-decisions
status: active
owner: {{ owner }}
created: "{{ created_at }}"
last_updated: "{{ updated_at }}"
```

Record key technical or process decisions made during this project.

## Log

* **DEC-001 ({{ created_at }}):** Initialized project workspace with Kanban flow-based memory.
