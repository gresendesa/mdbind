# B-014 - Translate project documentation to English and expand specification

- Status: done
- PO Priority: 2 (high)
- Risk: 2 (medium)
- Owner: gresendesa
- Creation date: 2026-06-06
- Last updated: 2026-06-06
- Dependencies: B-015 to B-025 (referenced in expanded spec)

## Description

Three goals:

1. **Translate** all user-facing Markdown files to English.
2. **Expand `specification.md`** with §9–§11 (done in SPR-2026-05).
3. **Rewrite `README.md`** with a clear positioning: mdgraph as structured
   memory in plain Markdown, for both humans and AI agents.

## Scope — Completed (SPR-2026-05)

- `CONSTITUTION.md` — translated ✅
- `scrum/backlog.md`, `sprint.md`, `decisions.md`, `experience.md` — translated ✅
- `specification.md` — translated + §9–§11 added ✅

## Scope — SPR-2026-08

- `README.md` — full rewrite with new positioning ✅
- `examples/README.md` — translated ✅
- `examples/wiki/*.md` — content and metadata translated ✅

## README positioning (PO directive)

Slogan: **Structured memory in plain Markdown.**

Do NOT position as a documentation tool. Position as:
- Markdown as structured memory storage for agents and humans
- Deterministic graph traversal over plain text
- Explicit relationships, stable URIs, inspectable knowledge

Avoid: "ultimate AI memory system", "revolutionary", "GraphQL for Markdown"

## Acceptance criteria

- README opens with slogan and positioning, not a tool description
- "Why?" section contrasts with vector DBs / opaque stores
- Quick example shows section + tree output
- Dedicated "Designed for AI memory" section
- Dedicated "Still useful without AI" section
- Philosophy section (5 principles)
- All commands documented with flags and examples
- No Portuguese content remaining

## Description

Two goals in one item:

1. **Translate** all user-facing Markdown files to English.
2. **Expand `specification.md`** with the mathematical specification and
   formal modeling of all new commands (B-015 to B-025).

Tests, `scrum/backlog/` and `scrum/sprint/` are excluded from translation.

## Scope — Translation

- `README.md` (primary focus)
- `examples/README.md`
- `examples/wiki/*.md` (all example files — content and metadata)
- `specification.md` (translated AND expanded, see below)

## Scope — specification.md expansion

The current spec covers sections 1–8. The expanded spec must add:

### Section 9 — Semantic Memory Model

Formal reframing of `mdgraph` as a **deterministic graph-based semantic
memory layer** for AI agents. Covers:

- Comparison with RAG (vector), knowledge graphs, Obsidian-style Markdown
- The stability property of `file.md#id` URIs vs. heading titles
- YAML as a first-class semantic index (not just decoration)
- The distinction between "document composition" (human output) and
  "context materialization" (AI input)

### Section 10 — Extended Graph Operations (mathematical model)

For each new command, define:

- **Formal signature**: $f: G \times V \rightarrow R$ (inputs and return type)
- **Algorithm**: BFS/DFS variant, set operations, path-finding
- **Complexity**: time and space $O(?)$
- **Output schema**: JSON structure

Commands to specify:

| Command | Algorithm | Notes |
|---------|-----------|-------|
| `validate` | Full index scan + edge verification | O(V+E) |
| `context` | 1-hop neighborhood extraction | O(deg(v)) |
| `backlinks` | Direct lookup in `incoming_edges` | O(1) |
| `search` | Linear scan with field predicate | O(V) |
| `impact` | Reverse BFS on `incoming_edges` | O(V+E) |
| `neighbors` | Bidirectional BFS | O(V+E) |
| `explain` | All simple paths (DFS) | O(V! worst case) |
| `diff` | Parse historical revision + set diff | O(V+E) |
| `query` | Parsed predicate + linear scan | O(V) |
| `context-compose` | Bounded compose with token budget | O(V+E) |

### Section 11 — JSON Contract

Defines the canonical JSON schema for every command's `--json` output.
This is the API contract for AI agent tool-calling integration.

## Acceptance criteria

- [ ] README.md fully translated to English
- [ ] examples/ fully translated to English
- [ ] specification.md translated to English
- [ ] specification.md sections 9-11 written in English
- [ ] Each new command has formal signature, algorithm, complexity and JSON schema
- [ ] No process/planning content in specification.md (roadmap, sprints, backlog refs)
- [ ] No changes to test files or scrum management files

