# B-029 - JSON context fails with YAML date metadata

- Status: doing
- PO Priority: 2 (high)
- Risk: 2 (medium)
- Owner: gresendesa
- Creation date: 2026-06-09
- Last updated: 2026-06-09
- Dependencies: none

## Description

Running `mdb context <uri> --root . --json` fails when a section metadata block
contains date-like YAML values such as `created_at: 2026-06-08`.

Observed error:

```text
TypeError: Object of type date is not JSON serializable
```

The expected behavior is that `--json` emits valid JSON even when YAML metadata
contains date values. Date-like metadata must be serialized as strings, or
metadata must otherwise be normalized into JSON-safe values before output.

`mdb context-compose <uri> --root . --json` works on the same repository, so the
bug is expected to be isolated to the `context` JSON serialization path or to
metadata normalization used by that command.

## Scope

- Reproduce the failure with a section metadata block containing a YAML date
  value.
- Add automated regression tests that fail before the fix and pass after it.
- Implement JSON-safe metadata normalization for `mdb context --json`.
- Preserve existing API/CLI contracts; no breaking output shape changes without
  versioning.
- Verify that `mdb context-compose --json` behavior remains unaffected.

## Criteria of acceptance

- [ ] `mdb context <uri> --root . --json` succeeds with metadata containing
      `created_at: 2026-06-08`.
- [ ] YAML date values in JSON output are serialized as strings, or are otherwise
      normalized into JSON-safe values.
- [ ] Regression tests cover the reported failure.
- [ ] Existing JSON tests for related commands still pass.
- [ ] `mdb context-compose <uri> --root . --json` remains working on equivalent
      metadata.

## Definition of Done

- [ ] Manual simulation of the reported problem documented.
- [ ] Tests created to reproduce the failure before the fix.
- [ ] Solution implemented.
- [ ] Simulation rerun after the fix and confirmed passing.
- [ ] Automated test suite executed successfully.
- [ ] Regression checklist executed and documented.
- [ ] Project memory updated under `scrum/`.

## Update history

- 2026-06-09: Item created from PO bug report.
- 2026-06-09: Selected for SPR-2026-11 with PO Priority 2 (high).
